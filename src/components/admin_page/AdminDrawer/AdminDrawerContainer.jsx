import React from "react";
import { useDispatch } from "react-redux";

const AdminDrawerContainer = () => {
  const dispatch = useDispatch();
  const dispatchToProps = {};
};
export default AdminDrawerContainer;
